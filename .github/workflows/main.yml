name: "Deploy to GAE"
on:
  push:
    tags:
      - v1.**
jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Compile Business card
        run: xelatex card.tex
